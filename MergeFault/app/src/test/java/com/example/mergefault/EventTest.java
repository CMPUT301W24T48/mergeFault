package com.example.mergefault;

public class EventTest {
    /*
    private final Uri testUri = Uri.fromFile(new File("MergeFault/app/src/main/res/drawable/eventposter.png"));
    private Calendar mockDateTime(){
        Calendar c = Calendar.getInstance();
        c.set(Calendar.DAY_OF_MONTH, 15);
        c.set(Calendar.MONTH, 3);
        c.set(Calendar.YEAR, 2024);
        c.set(Calendar.HOUR_OF_DAY,15);
        c.set(Calendar.MINUTE,30);
        return c;
    }

    private Event mockEvent(Calendar dateTime){
        return new Event("TestEvent", "1001", "Edmonton", dateTime, 25, testUri, "This is a test event", Boolean.TRUE, "12345");
    }
    @Test
    void testGetters(){
        Calendar dateTime = mockDateTime();
        Event event = mockEvent(dateTime);
        assertEquals("TestEvent", event.getEventName());
        assertEquals("1001", event.getOrganizerId());
        assertEquals("Edmonton", event.getLocation());
        assertEquals(dateTime , event.getDateTime());
        assertEquals((Integer) 25, event.getAttendeeLimit());
        assertEquals(testUri, event.getEventPoster());
        assertEquals("This is a test event", event.getDescription());
        assertEquals(Boolean.TRUE, event.getGeoLocOn());
        assertEquals("12345", event.getEventID());
    }
     */
}
